# Copy to .env.local and fill in. The key only ever lives server-side (used in
# src/app/api/agent/route.ts) — it is never shipped to the browser.
OPENAI_API_KEY=sk-...

# Optional: any vision + tool-calling chat model. Defaults to gpt-4o.
# OPENAI_MODEL=gpt-4o
